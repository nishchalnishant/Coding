# Python Snippets

These are intentionally short and interview-friendly.

Guidelines:
- Prefer clear variable names (`left`, `right`, `window_count`) over cleverness.
- Prefer iterative DFS/BFS when recursion depth might be large.
- Always state time/space after coding.

Start here: `../../SNIPPETS.md`
